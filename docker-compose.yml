services:
    homepage:
        build: .
        image: homepage
        restart: unless-stopped
        # Same host port as before, so the existing reverse proxy needs no change.
        ports:
            - "127.0.0.1:3001:80"
        networks:
            - homepage-app

networks:
    homepage-app:
        driver: bridge
